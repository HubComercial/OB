import os
from dotenv import load_dotenv

load_dotenv()

from modules.dst_detector import get_portugal_offset
from modules.otc_switcher import should_use_otc, should_use_backtest

OVERLAY_START = 8
OVERLAY_END = 16

POLLING_OVERLAY = 80
POLLING_OFF = 1800
END_OF_DAY_HOUR = 22

# ===== ATIVOS =====
ASSETS = [
    "EURUSD=X",
    "GBPUSD=X",
    "USDJPY=X",
    "AUDUSD=X",
    "USDCAD=X",
    "NZDUSD=X",
    "EURGBP=X",
    "EURJPY=X"
]

# ===== FALLBACKS PARA COMPATIBILIDADE =====
ASSET = ASSETS[0]
TIMEFRAME = 5
EXPIRY_TIME = 15   # <--- ADICIONADO

TIMEFRAMES = [5]

TIMEFRAME_CONFIGS = {
    5: {
        'ema_fast': 9,
        'ema_trend': 50,
        'rsi_low': 30,
        'rsi_high': 70,
        'atr_min': 0.0002,
        'bollinger_std': 1.8,
        'cci_threshold_buy': 80,
        'cci_threshold_sell': -80,
        'bollinger_tolerance': 0.0,
    },
}

OTC_WEEKEND = False
BACKTEST_WEEKEND = False
BACKTEST_CANDLES = 500

if should_use_otc():
    print("🔄 Modo OTC ativado (fim de semana). Usando ativos OTC.")
    ASSETS = [a.replace("=X", "-OTC") for a in ASSETS]
    ASSET = ASSETS[0]

if should_use_backtest():
    print("🧠 Modo BACKTEST ativado. O robô estudará dados históricos.")

AMOUNT = 1.0
MIN_INTERVAL = 16

DAILY_LOSS_LIMIT = -10.0
MAX_CONSECUTIVE_LOSSES = 3

CCI_PERIOD = 7
MACD_FAST = 12
MACD_SLOW = 26
MACD_SIGNAL = 9
BB_PERIOD = 20
VOLUME_PERIOD = 20

SENTIMENT_POSITIVE_THRESHOLD = 0.2
SENTIMENT_NEGATIVE_THRESHOLD = -0.2
SENTIMENT_STRONG_THRESHOLD = 0.6
ATR_PERIOD = 14
ATR_MAX = 0.005
ATR_ALERT_THRESHOLD = 0.0003
CCI_THRESHOLD = 100

ADX_MIN = 25
BOLLINGER_WIDTH_MIN = 0.0003

SCORE_WEIGHTS = {
    'ema_fast': 15,
    'ema_trend': 15,
    'macd': 15,
    'cci': 15,
    'rsi': 10,
    'volume': 10,
    'bollinger': 10,
    'sr': 5
}

MARKET_QUALITY_MIN = 50

CONFIRM_EXPIRY_MAP = {
    1: 2,
    5: 6,
    15: 16
}

IQ_EMAIL = os.getenv("IQ_EMAIL")
IQ_PASSWORD = os.getenv("IQ_PASSWORD")
NEWS_API_KEY = os.getenv("NEWS_API_KEY")
TELEGRAM_TOKEN = os.getenv("TELEGRAM_TOKEN")
TELEGRAM_CHAT_ID = os.getenv("TELEGRAM_CHAT_ID")
